# Build artifacts
__pycache__/
*.py[cod]
*.egg-info/
*.egg
# dist/   # UN-IGNORED: committed FE dist is needed for the self-contained image build
build/
.pytest_cache/
.coverage
htmlcov/

# Frontend build artifacts
src/frontend/node_modules/
# src/frontend/dist/   # UN-IGNORED: committed FE dist is needed for the self-contained image build
src/frontend/build.log
src/frontend/build.done
src/frontend/build.fail
src/frontend/*.tsbuildinfo

# State files (never bake into image)
*.sqlite
*.sqlite-journal
*.gguf
var/

# VCS + editor
.git/
.gitignore
.idea/
.vscode/
*.swp
*.swo

# Project meta + tests (kept for image — but `tests/` is large; comment to include)
tests/
ARCHITECTURE.md
docs/
